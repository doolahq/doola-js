---
'@doola/js': patch
---

The npm page now carries the full integration guide: keys and environments, the session route, mounting, the payment handoff, options, errors, Content Security Policy, browser support and lifecycle. Every TypeScript example in it is type-checked in CI.
